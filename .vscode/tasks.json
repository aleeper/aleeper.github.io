{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Site",
      "type": "shell",
      "command": "npm run build",
      "detail": "jemdoc in TeX Live Docker container, then assemble _site/",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
